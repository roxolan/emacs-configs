;; custom theme save file

(setq remember-theme-file "~/.emacs.d/emacs-theme")

;; turn off backup files

(setq make-backup-files nil)

;; debug on any error

(setq debug-on-error t)

(provide 'init-preload)
